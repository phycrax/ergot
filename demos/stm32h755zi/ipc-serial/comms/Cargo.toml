[package]
name = "comms"
version = "0.1.0"
edition = "2024"

[dependencies]
ergot   = { path = "../../../../crates/ergot" }
serde   = { version = "1.0", default-features = false, features = ["derive"] }
defmt = "1"